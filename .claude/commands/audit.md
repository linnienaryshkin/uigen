Your goal is to update any vulnerable dependencies

Do the following:

1. Run `npm audit` to identify vulnerable dependencies.
2. Run `npm audit fix` to automatically fix vulnerabilities where possible.
3. Run tests to ensure that the fixes do not break the application.
